# ===========================================
# SHARED INFRASTRUCTURE VARIABLES
# ===========================================

# Redis Configuration
REDIS_URL=redis://redis:6379/0
REDIS_RATE_LIMIT_URL=redis://redis:6379/0

# Traefik Configuration
TRAEFIK_DASHBOARD_PORT=8080
TRAEFIK_HTTP_PORT=80
TRAEFIK_VERSION=v3

# Network Configuration
NETWORK_NAME=shared-network

# Common Settings
LOG_LEVEL=info

# Project Name - Used as prefix for all containers, networks, and volumes
# Change this to avoid conflicts with other Docker stacks on your system
# Example: COMPOSE_PROJECT_NAME=my-dev-stack
COMPOSE_PROJECT_NAME=my-stack

# Domain Configuration (adjust as needed)
BASE_DOMAIN=localhost

# ===========================================
# FASTAPI APPLICATION VARIABLES
# ===========================================

# FastAPI Build Mode
# Controls the build and runtime behavior of the FastAPI application.
# - production: Installs production dependencies, runs with a single worker.
# - development: Installs dev dependencies, runs with hot reload for development.
BUILD_MODE=production