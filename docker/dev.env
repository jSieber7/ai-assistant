# Build mode for the FastAPI app
BUILD_MODE=development

# Port for the FastAPI app
APP_PORT=8000

# Base domain for Traefik routing
BASE_DOMAIN=localhost

# Compose project name
COMPOSE_PROJECT_NAME=my-stack-dev

# Certificate resolver for Traefik (empty for local dev to use default self-signed cert)
TLS_CERT_RESOLVER=